---
import EntityLink from "./EntityLink.astro";
import type { DictionaryBundle } from "~/lib/bundle";
import type { ValueTermNode } from "~/lib/types";

interface Props {
  slug: string;
  bundle: DictionaryBundle;
  terms: readonly ValueTermNode[];
}

const { slug, bundle, terms } = Astro.props;
---

{terms.length === 0 ? (
  <p class="text-sm text-ink-500">No terms.</p>
) : (
  <div class="overflow-x-auto scroll-thin -mx-2">
    <table class="min-w-full text-sm border-collapse">
      <thead>
        <tr class="border-b border-ink-200 text-left text-xs uppercase tracking-wide text-ink-500">
          <th scope="col" class="px-3 py-2 font-medium">Code</th>
          <th scope="col" class="px-3 py-2 font-medium">Name</th>
          <th scope="col" class="px-3 py-2 font-medium">Definition</th>
        </tr>
      </thead>
      <tbody>
        {terms.map((t) => (
          <tr class="border-b border-ink-100 align-top transition hover:bg-sand-50/60">
            <td class="px-3 py-2 font-mono text-[11px] text-ink-600 whitespace-nowrap">
              {t.enumeration_code ?? t.code}
            </td>
            <td class="px-3 py-2 font-medium text-ink-800">
              <EntityLink slug={slug} irdi={t.irdi} bundle={bundle} />
            </td>
            <td class="px-3 py-2 text-xs text-ink-600">
              {t.definition ?? <span class="text-ink-300">—</span>}
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
)}
